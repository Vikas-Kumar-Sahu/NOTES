1.WAQTD NAMES OF AN EMP WORKING IN NEW YORK?
SELECT ENAME
FROM EMP
WHERE DEPTNO = (SELECT DEPTNO
		FROM DEPT
		WHERE LOC = 'NEW YORK');
ENAME
-------
CLARK
KING
MILLER

2.WAQTD MAX SAL IN REASERCH DEPT?
SELECT MAX(SAL)
FROM EMP
WHERE DEPTNO = (SELECT DEPTNO
		FROM DEPT
		WHERE DNAME='RESEARCH');
  MAX(SAL)
----------
      3000

3.WAQTD MIN SAL IN ACCOUNTING DEPT?
SELECT MIN(SAL)
FROM EMP
WHERE DEPTNO = (SELECT DEPTNO
		FROM DEPT
		WHERE DNAME='ACCOUNTING');
  MIN(SAL)
----------
      1300

4.WAQTD THE DNAME OF PRESIDENT?
SELECT DNAME
FROM DEPT
WHERE DEPTNO = (SELECT DEPTNO
		FROM EMP
		WHERE JOB='PRESIDENT');
DNAME
--------------
ACCOUNTING

5.WAQTD LOC OF AN EMP HIRED BEFORE 1981?
SELECT LOC
FROM DEPT
WHERE DEPTNO = (SELECT DEPTNO
		FROM EMP
		WHERE HIREDATE<'01-JAN-1981');
LOC
-------------
DALLAS

6.WAQTD NAME OF AN EMP WORKING AS MANAGER IN RESEARCH DEPT?
SELECT ENAME
FROM EMP
WHERE JOB = 'MANAGER' AND DEPTNO = (SELECT DEPTNO
				FROM DEPT
				WHERE DNAME = 'RESEARCH');
ENAME
---------
JONES

7.WAQTD NAME OF THE CLERK IN CHICAGO?
SELECT ENAME
FROM EMP
WHERE JOB = 'CLERK' AND DEPTNO = (SELECT DEPTNO
				FROM DEPT
				WHERE LOC = 'CHICAGO');
ENAME
----------
JAMES

8.WAQTD NAME OF AN EMP WHO'S NAME START WITH CHAR A IN RESEARCH DEPT?
SELECT ENAME
FROM EMP
WHERE ENAME LIKE 'A%' AND DEPTNO = (SELECT DEPTNO
				FROM DEPT
				WHERE DNAME = 'RESEARCH');
ENAME
----------
ADAMS

9.WAQTD NAMES OF AN EMP HIRED AFTER 1981 IN ACCOUNTING DEPT?
SELECT ENAME
FROM EMP
WHERE HIREDATE > '31-DEC-1981' AND DEPTNO = (SELECT DEPTNO
				FROM DEPT
				WHERE DNAME = 'ACCOUNTING');
ENAME
----------
MILLER

10.WAQTD NAME OF AN EMP HIRED IN MONTH OD DEC IN NEW YORK AND CHICAGO?
SELECT ENAME
FROM EMP
WHERE HIREDATE LIKE '%DEC%' AND DEPTNO IN (SELECT DEPTNO
				FROM DEPT
				WHERE LOC IN ('NEW YORK','CHICAGO'));
ENAME
----------
JAMES

11.WAQTD NAME OF AN EMP HIRED IN THE YEAR 1981 IN RESEARCH DEPT?
SELECT ENAME
FROM EMP
WHERE HIREDATE BETWEEN '01-JAN-81' AND '31-DEC-81' AND DEPTNO IN (SELECT DEPTNO
				FROM DEPT
				WHERE DNAME = 'RESEARCH');
ENAME
----------
JONES
FORD

12.WAQTD NUMBER OF EMP WORKING IN THE DNAME HAVING 4TH CHARACTER E?
SELECT COUNT(*)
FROM EMP
WHERE DEPTNO IN (SELECT DEPTNO
		FROM DEPT
		WHERE DNAME LIKE '___E%');
  COUNT(*)
----------
        11

13.WAQTD NAME OF AN EMP EARNING LESS THAN 2000 IN NEW YORK?
SELECT ENAME
FROM EMP
WHERE SAL < 2000 AND DEPTNO IN (SELECT DEPTNO
				FROM DEPT
				WHERE LOC = 'NEW YORK');
ENAME
----------
MILLER

14.WAQTD NAME OF AN EMP WORKING IN SAME DESIGNATION AS THAT OF SMITH IN CHICAGO?
SELECT ENAME
FROM EMP
WHERE DEPTNO IN (SELECT DEPTNO
		FROM DEPT
		WHERE LOC = 'CHICAGO')
AND JOB = (SELECT JOB
		FROM EMP
		WHERE ENAME = 'SMITH');
ENAME
----------
JAMES

15.WAQTD NAMES OF EMP EARNING MORE THAN JONES IN RESEARCH DEPT?
SELECT ENAME
FROM EMP
WHERE DEPTNO IN (SELECT DEPTNO
		FROM DEPT
		WHERE DNAME = 'RESEARCH')
AND SAL > (SELECT SAL
		FROM EMP
		WHERE ENAME = 'JONES');
ENAME
----------
SCOTT
FORD

16.WAQTD NAME OF AN EMP HIRED BEFORE MILLER IN ACCOUNTING DEPT?
SELECT ENAME
FROM EMP
WHERE DEPTNO IN (SELECT DEPTNO
		FROM DEPT
		WHERE DNAME = 'ACCOUNTING')
AND HIREDATE < (SELECT HIREDATE
		FROM EMP
		WHERE ENAME = 'MILLER');
ENAME
----------
CLARK
KING

17.WAQTD NUMBER OF EMP WORKING IN SAME DESIGNATION AS THAT OF SCOTT IN RESEARCH OR SALES DEPT?
SELECT COUNT(*)
FROM EMP
WHERE DEPTNO IN (SELECT DEPTNO
		FROM DEPT
		WHERE DNAME IN ('RESEARCH','SALES'))
AND JOB IN (SELECT JOB
		FROM EMP
		WHERE ENAME = 'SCOTT');
  COUNT(*)
----------
         2

18.WAQTD MAX SAL GIVEN TO EMP WORKING IN JAMES DESIGNATION IN DALLLAS?
SELECT MAX(SAL)
FROM EMP
WHERE DEPTNO IN (SELECT DEPTNO
		FROM DEPT
		WHERE LOC = 'DALLAS')
AND JOB IN (SELECT JOB
		FROM EMP
		WHERE ENAME = 'JAMES');
  MAX(SAL)
----------
      1100

19.WAQTD NAME OF AN EMP WORKING IN SAME DESIGNATION AS THAT OF SMITH AND HIRED BEFORE ADAMS BUT EARNING LESS THAN MILLER AND HIRED IN THE MONTH OF DEC IN RESEARCH OR ACCOUNTING DEPT?
SELECT ENAME
FROM EMP
WHERE DEPTNO IN (SELECT DEPTNO
		FROM DEPT
		WHERE DNAME IN ('RESEARCH','ACCOUNTING'))
AND JOB IN (SELECT JOB
		FROM EMP
		WHERE ENAME = 'SMITH')
AND HIREDATE < (SELECT HIREDATE
		FROM EMP
		WHERE ENAME = 'ADAMS')
AND SAL < (SELECT SAL
		FROM EMP
		WHERE ENAME = 'MILLER')
AND HIREDATE LIKE '%DEC%';
ENAME
----------
SMITH

20.WAQTD DETAILS OF EMP WORKING AS SAME DESIGNATION AS THAT OF JONES AND HIRED IN MONTH OF MAY OR JULY OR JUNE AND EARNING MORE THAN 2000 BUT LESS THAN JONES AND HIS NAME MUST HAVE 3RD CHAR A AND HIS SALARY MUST END WITH 50 IN NEW YORK OR DALLAS?
SELECT *
FROM EMP
WHERE JOB = (SELECT JOB
		FROM EMP
		WHERE ENAME = 'JONES')
AND (HIREDATE LIKE '%MAY%' OR HIREDATE LIKE '%JUL%' OR HIREDATE LIKE '%JUN%')
AND SAL > 2000
AND SAL < (SELECT SAL
		FROM EMP
		WHERE ENAME = 'JONES')
AND ENAME LIKE '__A%'
AND SAL LIKE '%50'
AND DEPTNO IN (SELECT DEPTNO
		FROM DEPT
		WHERE LOC IN ('NEW YORK','DALLAS'));
     EMPNO ENAME      JOB              MGR HIREDATE         SAL       COMM     DEPTNO
---------- ---------- --------- ---------- --------- ---------- ---------- ----------
      7782 CLARK      MANAGER         7839 09-JUN-81       2450                    10

21.WAQTD THE MAX SAL?
SELECT MAX(SAL)
FROM EMP;
  MAX(SAL)
----------
      5000

22.WAQTD NAME OF AN EMP EARNING MAX SAL?
SELECT ENAME
FROM EMP
WHERE SAL = (SELECT MAX(SAL)
		FROM EMP);
ENAME
----------
KING

23.WAQTD 2ND MAX SAL?
SELECT MAX(SAL)
FROM EMP
WHERE SAL < (SELECT MAX(SAL)
		FROM EMP);
  MAX(SAL)
----------
      3000

24.WAQTD NAME OF AN EMP EARNING 2ND MAX SAL?
SELECT ENAME
FROM EMP
WHERE SAL = (SELECT MAX(SAL)
		FROM EMP 
		WHERE SAL < (SELECT MAX(SAL)
				FROM EMP));
ENAME
----------
SCOTT
FORD

25.WAQTD DNAME OF EMP EARNING THE MAXIMUM SAL? 
SELECT DNAME
FROM DEPT
WHERE DEPTNO = (SELECT DEPTNO
		FROM EMP
		WHERE SAL = (SELECT MAX(SAL)
				FROM EMP));
DNAME
--------------
ACCOUNTING