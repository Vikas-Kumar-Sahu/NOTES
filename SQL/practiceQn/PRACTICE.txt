 SELECT ENAME
 FROM EMP
 WHERE JOB = (SELECT JOB
              FROM EMP
              WHERE ENAME = 'MILLER');

SELECT COUNT(*)
FROM EMP
WHERE DEPTNO = (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'SCOTT')
GROUP BY DEPTNO;

SELECT MAX(SAL)
FROM EMP
WHERE DEPTNO = (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'ALLEN')
GROUP BY DEPTNO;

SELECT AVG(SAL)
FROM EMP
WHERE JOB = (SELECT JOB
              FROM EMP
              WHERE ENAME = 'SCOTT')
GROUP BY SAL;

SELECT ENAME
FROM EMP
WHERE SAL < (SELECT SAL
              FROM EMP
              WHERE ENAME = 'JAMES');

SELECT *
FROM EMP
WHERE JOB IN ('CLERK') AND DEPTNO IN (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'KING');


SELECT ENAME
FROM EMP
WHERE JOB = 'MANAGER' AND DEPTNO IN (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'SMITH');

SELECT ENAME
FROM EMP
WHERE HIREDATE>'31-DEC-1981' AND DEPTNO IN (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'KING');

SELECT *
FROM EMP
WHERE HIREDATE BETWEEN '01-JAN-1981' AND '31-DEC-1981' AND DEPTNO IN (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'SCOTT');

SELECT ENAME
FROM EMP
WHERE ENAME LIKE 'A%' AND JOB IN (SELECT JOB
              FROM EMP
              WHERE ENAME = 'MILLER');

SELECT *
FROM EMP
WHERE ENAME LIKE '%S' AND DEPTNO IN (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'MARTIN');

SELECT *
FROM EMP
WHERE HIREDATE LIKE '%DEC%' AND JOB IN (SELECT JOB
              FROM EMP
              WHERE ENAME = 'MILLER');

SELECT *
FROM EMP
WHERE SAL > 2000 AND DEPTNO IN (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'KING');

SELECT *
FROM EMP
WHERE HIREDATE>'31-DEC-1981' AND HIREDATE < (SELECT HIREDATE
              FROM EMP
              WHERE ENAME = 'ADAMS');

SELECT *
FROM EMP
WHERE SAL > (SELECT SAL
              FROM EMP
              WHERE ENAME = 'JONES')
AND SAL < (SELECT SAL
              FROM EMP
              WHERE ENAME = 'KING');

SELECT *
FROM EMP
WHERE HIREDATE > (SELECT HIREDATE
              FROM EMP
              WHERE ENAME = 'MILLER')
AND HIREDATE < (SELECT HIREDATE
              FROM EMP
              WHERE ENAME = 'ADAMS');

SELECT *
FROM EMP
WHERE JOB = (SELECT JOB
              FROM EMP
              WHERE ENAME = 'SMITH')
AND DEPTNO = (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'KING');

SELECT *
FROM EMP
WHERE JOB = (SELECT JOB
              FROM EMP
              WHERE ENAME = 'BLAKE')
AND DEPTNO = (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'SMITH');

SELECT *
FROM EMP
WHERE SAL < (SELECT SAL
              FROM EMP
              WHERE ENAME = 'ALLEN')
AND DEPTNO = (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'KING');

SELECT *
FROM EMP
WHERE HIREDATE < (SELECT HIREDATE
              FROM EMP
              WHERE ENAME = 'MILLER')
AND DEPTNO = (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'SCOTT');

SELECT *
FROM EMP
WHERE HIREDATE < (SELECT HIREDATE
              FROM EMP
              WHERE ENAME = 'MARTIN')
AND HIREDATE LIKE '%DEC%'
AND DEPTNO = (SELECT DEPTNO
              FROM EMP
              WHERE ENAME = 'FORD')
AND SAL < (SELECT SAL
              FROM EMP
              WHERE ENAME = 'WARD')
AND JOB = (SELECT JOB
              FROM EMP
              WHERE ENAME = 'MILLER');

SELECT ENAME, SAL
FROM EMP
WHERE SAL = (SELECT MAX(SAL)
		FROM EMP);

SELECT ENAME
FROM EMP
WHERE HIREDATE = (SELECT MIN(HIREDATE)
		FROM EMP);

SELECT ENAME
FROM EMP
WHERE HIREDATE = (SELECT MAX(HIREDATE)
		FROM EMP);

SELECT MAX(SAL)
FROM EMP
WHERE JOB = (SELECT JOB
	     FROM EMP
	     WHERE ENAME = 'MILLER')
AND DEPTNO = (SELECT DEPTNO
	     FROM EMP
	     WHERE ENAME = 'SCOTT');